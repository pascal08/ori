parameters:
    # for "merge" command
    data_to_append:
        require-dev:
            phpunit/phpunit: '^7.3'
            symplify/monorepo-builder: '^4.7'

    # for "split" command
    directories_to_repositories:
        packages/Container: 'git@github.com:pascal08/container.git'
        packages/EventSubscriber: 'git@github.com:pascal08/event-subscriber.git'
        packages/Router: 'git@github.com:pascal08/router.git'
        packages/TaskScheduler: 'git@github.com:pascal08/task-scheduler.git'
